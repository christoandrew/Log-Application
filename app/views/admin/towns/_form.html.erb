<%= simple_form_for([:admin, @town]) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :name %>
      <%= f.input :area_code %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
<% end %>
